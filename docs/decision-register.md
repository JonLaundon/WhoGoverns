# Decision register

**Mirrored, version-controlled copy** of §10 of the governing plan (`../../2026-07-05-state-machine-project-plan-v0-5.md`), which lives outside this repo in the OneDrive workspace. The plan remains the authoritative source; this copy exists so the register is under Git like the rest of the build. **Do not edit this file directly** — run `pipeline/sync_decision_register.py` after the plan's §10 changes.

Detailed engineering rationale for the powers-layer decisions is in `../issues/schema-decisions.md`; this file is the index of truth (decision, default, status).

| # | Decision | Default proposed | Status |
|---|---|---|---|
| 1 | Publication entity: personal GitHub, neutral organisation account, or hosted by the Centre for British Progress | Neutral organisation account | **Closed 2026-07-05 — personal GitHub (interim); build/compile in folder, push Spiral 1 as a single first push at exit** |
| 2 | Spiral 2 exit retrodiction case | Energy nationally significant infrastructure project (aligns with the Centre for British Progress's focus) | **Closed 2026-07-14 — the Ofwat ~£123m Thames Water penalty (sewage-spill failures + unjustified dividends).** A reservoir was rejected: it is consented via the NSIP/planning regime (not built until Tranche A), so it fails Water's self-containment. This generalises retrodiction for a regulator-first domain from "blocked project" to "which levers bit, which didn't" — and dovetails with the renationalisation demo. River Wye deferred (sprawls into planning + agriculture + devolved NRW). See `2026-07-14-spiral-2-approach-and-mog-review.md` §12. |
| 3 | Verification depth | Single-check baseline + double-check for Annex A10.4 high-risk categories | **Confirmed 2026-07-06 (sponsor) — the default stands** |
| 4 | Project name and public identity | "The State Machine" (working title) | **Closed 2026-07-05 — public name "WhoGoverns"; folder/working name "His Majesty's Government"** |
| 5 | Model Context Protocol server timing | Spiral 4, unless pipeline demand pulls it into Spiral 3 for internal use | Default pending |
| 6 | Compiled query layer | SQLite primary; graph database deferred unless chain queries demand it | Default pending |
| 7 | Legislation snapshots stored locally versus referenced externally | Reference externally + store point-in-time version date (Annex A2.6); local snapshots only for launch-critical sources | Default pending |
| 8 | Public launch timing | After Tranche A completes (a structural-only earlier launch was rejected: it invites "just another org chart" framing — the Spiral 1 parity build is visible but not promoted as a launch) | Default pending |
| 9 | JSON Schema exact field requirements | Settled by hand-building three records pre-Spiral 1 (Annex A13.1) | In progress by design |
| 10 | Tranche A0 pipeline-priority body list (10–15 bodies) | Sponsor selects from the policy areas the review pipeline is actively working | **Domain fixed 2026-07-14 — Water / Ofwat** (pipeline-live: incoming-PM water renationalisation; self-contained, one dominant Act). Pipeline live areas: crime/justice, NHS, civil-service reform, energy — Water chosen first; Energy = Tranche A next. The exact 10–15 body/office list (Ofwat, EA, DWI, Defra, CCW, Natural England, + offices) is enumerated at build start. Methodology: domain-first, holder-first delivery, law-first extraction (`2026-07-14-spiral-2-approach-and-mog-review.md` §4, §11–12). · **In progress 2026-07-21 — first-pass extraction underway.** Water tranche now ~22 powers, 11 duties, 10 vetoes across Ofwat, EA, Natural England, CMA, Treasury, Chancery + the Defra SoS office; compiled onto the map (card + `can_veto`/`must_consult` edges). Explicitly NOT complete: `issues/breadcrumbs.md` tracks ~10 unmined provisions + adjacent instruments; Environment Act 2021 storm-overflows not yet registered. "First-pass complete on extracted sections", not whole-statute. |
| 11 | Spiral 2 first-pass extraction scope | Founding Act plus principal instruments per body; full statute sweep as a scheduled second pass | Default pending |
| 12 | Body classification taxonomy (`body_type`) | Re-base on the machineryofgovernment.uk category scheme (pattern only, no data reuse) | **Closed 2026-07-06 — `body_type` v0.2 (10 terms): dropped `regulator` (regulation is a function, not a type); renamed to `executive_ndpb`/`advisory_ndpb`/`other_body`; added `division_directorate` (API "Sub organisation") and `royal_charter_body`. `royal_charter_body` is populated from a Privy Council tranche, NOT the GOV.UK Organisations API (chartered bodies are absent from it). Advanced from Spiral 2 because the full ingest forced it. Annex A v0.4; see `whogoverns/issues/schema-decisions.md`. · Amended 2026-07-06 (Spiral 1.5): the functional axis (#15) is the additive home for "regulator" and other cross-cutting groupings; #12's dropping of `regulator` as a `body_type` stands.** |
| 13 | Officials/offices in the powers model | Powers vest in bodies | **Closed 2026-07-06 — offices are first-class, power-holding nodes (corporations sole), NOT a `body_type`. Power/Duty/Veto gain a polymorphic `holder` (`holder_type` office\|body, `office_id`) — Annex A6 v0.4; built when they leave draft in Spiral 2. Spiral 1 map is office-centred (PM → Cabinet → ministers → departments → ALBs).** |
| 14 | When to add senior officials (civil servants, independent officials) | Spiral 5 (senior officials/appointments graph) | **Closed 2026-07-06 — pulled forward: independent officials (heads of the non-ministerial departments) and departmental permanent secretaries hold statutory powers/veto points the Spiral 2 register needs, so a first tranche is built now (structure) with powers in Spiral 2. Scoped tight: non-min dept heads → `independent_official`, dept perm secs → `civil_servant`; 34 built from the CO directory + GOV.UK content-API board members. Deeper senior-civil-service graph (data.gov.uk organograms) remains Spiral 5.** |
| 15 | Cross-cutting groupings (regulators etc.) that span several `body_type`s | Add a second, multi-valued functional axis rather than reopen #12 | **Closed 2026-07-06 (Spiral 1.5) — `functions[]` (+ `function_source_ids[]`) added to Body, orthogonal to `body_type`. The data forced it: regulators span four `body_type`s (Ofgem/CMA non-min-dept, Ofcom/FCA other_body, CQC/ICO exec-NDPB, ONR public corp). Vocab `functions.json` v0.1 (10 terms); populated `regulation` only, from the DBT List of UK regulators (official, OGL v3.0), exact-match, 94 bodies tagged and each cited. Other terms defined but unpopulated; in Spiral 2 the tag becomes derivable from statutory powers. Map: regulator highlight + Functions legend index. See `whogoverns/issues/schema-decisions.md`.** |
| 16 | Bodies outside the GOV.UK Organisations API (chartered bodies; independent statutory regulators) | Add a primary-sourced off-register tranche, scoped by the statutory-function test | **Closed 2026-07-06 (Spiral 1.5) — 22 records via `ingest_offregister.py`. (A) `royal_charter_body` populated (5: RCVS, CWGC, Royal Society, British Academy, Royal Academy of Engineering), sourced to the Privy Council register — the 8th outer ring on the map. (B) 17 independent statutory regulators (8 health + 7 legal professional regulators, Farriers, Takeover Panel) as `other_body` + review flag + `functions:["regulation"]`, sourced to the DBT list. Scope = statutory-function test: include off-register bodies with a statutory public function; exclude private/voluntary chartered institutes (accountancy AML supervisors). Devolved regulators, parliamentary/Crown bodies and internal departmental units parked as separate categories. Founding statutes/charter provisions are Spiral 2 citations. record_status=extracted; awaits sponsor verification.** |
| 17 | Finer classification of the `other_body` catch-all (203 bodies) and courts | ONS Public Sector Classification Guide (rank source); possible dedicated `court` class | Open — scheduled refinement (Spiral 5 candidate); logged in `whogoverns/issues/source-gaps.md` |
| 18 | Veto as a schema primitive vs a consequence of power | — | **Closed 2026-07-14 — Veto is a materialised DERIVED projection of the consent/approval/concurrence/call-in/objection/licence-refusal/designation Power family, not a fifth primitive.** `derived_from_record_id` (canonical `power-*`) + `decision_affected` are required on a Veto (schema-enforced). MoG has no veto concept (all "Power"); we materialise it because "who can block?" is the flagship query and the Veto carries the block target. Annex A6 amended; enforced by `whogoverns/schemas/veto.schema.json`. |
| 19 | A MoG-style "Responsibility" record type | — | **Closed 2026-07-14 — NO Responsibility record type.** Responsibilities decompose into powers+duties or live in the body summary / `/analysis` layer, never as uncited `/data` (A2.2, A2.4). Keeps the register a cited primary-source product; MoG can carry soft Responsibility entries because it is editorial. |
| 20 | Verification assurance tiers (throughput vs the "no publication without verification" rule) | — | **Closed 2026-07-14 — tiered `verification_status`: `unverified` → `machine_verified` (passed the schema-blind eval, no human) → `single_checked`/`double_checked` (human, A10).** Machine-verified records may publish WITH an honest reader-visible label (uses the record-status transparency built into the map); high-impact vetoes still require a human double-check (decision #3). The CBP grant funds the human upgrade path. Escalation: eval → red-flag → manual/second-LLM → grant-funded human. · **Method exercised 2026-07-20:** a 3-tier strength audit (internal-coherence gate in `validate.py` → primary-source override sweep → independent schema-blind re-grading) re-graded 5 of 8 Water vetoes and raised all 8 to `machine_verified` on 8/8 blind agreement. The strength vocab now carries definitions anchored to JOYS 6th ed. §§2.57-2.60. NB: no record is human-checked yet — the honest floor for the whole tranche remains `machine_verified`/`unverified`. |
| 21 | Statutory vs prerogative/common-law powers in the model | — | **Closed 2026-07-15 — `power_basis` (statutory/prerogative/common_law/contractual, required) added to the Power schema.** Spiral 2 populates `statutory` only; the field makes a statutorily-thin domain read as *"prerogative likely"* not *"no power"*. Prerogative is a live common-law power held in abeyance where statute occupies the field (*De Keyser* 1920; *Fire Brigades Union* 1995) — a real blocker until displaced by statute, not by non-exercise. It has no provision to cite, so its sourcing route (Cabinet Manual, MoJ 2009 prerogative review, case law) and population are deferred past Spiral 2. Governance note: any future prerogative population in MoD/FCDO/intelligence is closer to constitutional interpretation than statute-transcription — sponsor confirms the recusal line holds before that work. Annex A6.3 amended; `whogoverns/schemas/power.schema.json`. |
| 22 | Constitutional conventions (Sewel, collective responsibility, dissolution advice) in the model | — | **Closed 2026-07-15 — conventions are NOT legal powers and are never `/data` records; never a Veto** (our veto definition is a *legally meaningful* block; conventions are non-justiciable — *Miller No.1* on Sewel). If modelled at all, they belong in the Annex B analysis layer as a walled-off, enforceability-marked category (Spiral 5 candidate). Flag Sewel specifically: a devolved-consent block will surface as a genuine gap in Water/Energy tranches. |
| 23 | Retained/assimilated EU law, international treaties, and the ECHR | — | **Closed 2026-07-15 — assimilated (retained-EU) law is IN scope by default** — it lives on legislation.gov.uk as ordinary domestic SIs, so the extractor gets it if it reads secondary legislation, not just Acts; add an EU-origin flag on the Source record (populated via the government assimilated-law dashboard as a discovery aid, records still cite legislation.gov.uk) and prioritise these in the currency job (most actively churned under the 2023 Act). **Treaties stay OUT as nodes** (UK dualist — a treaty confers no domestic power until an Act implements it); optional narrative `originating_instrument` note only. **The ECHR is NOT EU law** (Council of Europe; Strasbourg ≠ CJEU) — it enters as `statutory` through the Human Rights Act 1998 (s.6 public-authority constraint; s.4 declaration of incompatibility, which is not a strike-down — Parliament stays sovereign). The HRA is a candidate cross-cutting Instrument for a future **Home Office / immigration tranche**, not Water. |
| 24 | Status of the Re:State "Over Ruled" Appendix (115 statutory regulators + founding legislation) | — | **Closed 2026-07-15 — DISCOVERY SOURCE ONLY (A2.1).** The Appendix is a third-party compilation: use it for coverage cross-check, gap-finding and citation leads, never as a `source_id`. Every founding-Act it names is re-fetched from legislation.gov.uk (the primary source); Berman/Re:State recorded as discovery provenance at most. Use is *differential, not import*: run our own LLM pass, then where it and the 115-list disagree, treat the difference as signal (a body we missed / one his five-part test includes that our decisions #12/#15/#16 classify out — e.g. accountancy-AML supervisors, already excluded / a genuine judgement boundary). Do not present coverage-of-Berman as coverage-of-"the" register (his scope excludes local, devolved-only, advisory bodies; ours is whole-state). The regulator-register view is a wedge/front door, not the product — the powers/veto layer is the moat. |
| 25 | Sequencing: Over Ruled register-seed vs Water calibration | — | **Closed 2026-07-15 — WATER-FIRST.** Continue the Water powers calibration now; use only the Appendix's water rows (DWI/Ofwat/EA/CMA/Natural England) as a differential cross-check on our founding-instrument citations. Defer the full 115-body reconciliation + the Rec-1 filter fields (#26) to a later scoped Spiral-1.5-style structural pass. Protects the Spiral 2 value-test (one contested domain, one real user, external validation); external artefact/outreach waits. |
| 26 | Rec-1 register filter axes (`jurisdiction`, `ministerial_independence`) on Body | ONS/DBT sources + the five-part definition as reference | Open — Spiral 5 candidate; surfaced by "Over Ruled" Rec 1 (filterable by sector, form, jurisdiction, degree of ministerial independence, function). We carry `body_type` + `functions[]` but not these two axes. Logged, not built. |
| 27 | Cross-reference "breadcrumb" mining as a method step | — | **Closed 2026-07-20 — added to the extraction method (AGENT.md).** Capture each provision's outbound references in `Provision.references`; any reference pointing OUT of the domain's core instruments flags a candidate adjacent instrument (planning/DCO, Habitats Regs, Reservoirs Act). This is how the net widens to left-field blockers without sweeping the whole statute book. The decision-first retrodiction is the completeness AUDIT. Systematic population of `Provision.references` during extraction is the follow-on (currently noted in record `provenance_note`s). · **Follow-on now closed by #33 (2026-07-20): `Provision.references` population is built and automatic.** |
| 28 | Statutory office-holders and sub-units (DWI, Chief Inspector, inspectorates) in the model | — | **DWI part CLOSED 2026-07-20 (sponsor): a sub-unit with no separate legal personality is NOT a body node** — its powers attach to the parent department's holder, tagged to the function (a "micro-department" partition). Vindicated by WIA s.86 (DWI inspectors act "on [the SoS's] behalf"): DWI's s.70 enforcement power is held by the Defra office. **Broader framework CLOSED 2026-07-20 (sponsor): no new ring/entity for statutory office-holders — reuse the existing Office primitive (decision #13), which already models "a statutory post that holds power, distinct from body and person".** The modelling test (read off the enabling words): power **vested in the post** → an Office node (parent = host body); power **delegated "on behalf of" the parent** → parent holder + function tag, no node. Precedent already set by decision #14 (non-min-dept heads as `independent_official` offices). Render offices in the viewer as individual/office markers partitioned WITHIN their host body, not as peer rings (no top-level clutter). An `office_type` value for non-ministerial statutory office-holders may be added if `independent_official` does not stretch. Awkward tail case (accredited private individuals with statutory functions, e.g. Reservoirs Act 1975 panel engineers) → model the function on the overseeing body with a note, not per-individual nodes; decided when reached via breadcrumb. Devolved administrations and local government stay Spiral 5. |
| 29 | How private-party blocks/duties appear (the state-vs-private boundary on the map) | — | **Closed 2026-07-20 (sponsor) — the MAP models what the state does to ITSELF; the CARD and QUERY carry the whole field.** A veto or duty whose counterparty is a private person/company or an unnamed class draws NO edge (flagged `blocks_party_outside_state` / null `owed_to_*`), because there is no state node at the other end — but it stays fully visible on the entity card and answers "can I do this?" with a citation. Sponsor caveat: NOT showing these must not read as *absolute libertarianism* (the state does constrain private actors). **OPEN follow-on: a second, state→private view** (the "another map in development") to surface these relationships explicitly. Card + query done; the view is a design item. |
| 30 | Veto target field (which record a veto obstructs) | — | **Closed 2026-07-20 — `blocks_record_id` + `blocks_record_type` (`power`\|`duty`), polymorphic.** Generalised from an earlier `blocks_power_id` after the WIA s.16 trawl proved a veto can obstruct the discharge of a DUTY, not only a power (s.16A blocks the s.16 duty to modify). `derived_from_record_id` stays PROVENANCE (canonical power at the veto's own provision); `blocks_record_id` is the TARGET, populated uniformly. Two statutory shapes (embedded consent / free-standing block) both correct, derived in compile as `projection_shape`, never stored. Validate checks polymorphically. `whogoverns/schemas/veto.schema.json`. |
| 31 | Duty counterparty + consultation as a traversable relationship (U13) | — | **Closed 2026-07-20 — `owed_to_body_id`/`owed_to_office_id`/`owed_to_holder_type` on Duty; compile emits a `must_consult` edge.** A consultation duty is a relationship, as traversable as a veto; without this, "which bodies must legally be consulted before X?" (U13) and the consultation-duty third of U1 could only be read from prose. Populated only where the text names a state actor (else null → no edge, same rule as #29). Finding: a `consultation` duty whose consultee is a PRIVATE company (WIA s.22A) proves `duty_type` alone cannot imply an edge. `whogoverns/schemas/duty.schema.json`. |
| 32 | Where `functions[]` values come from (the judicial layer + derivation) | Derivable from statutory powers (AGENT.md) | **Closed 2026-07-20 — three routes, all keeping the tag traceable to cited provisions.** (a) SOURCED: `functions.json` **v0.2** adds `judicial` (held separately from `tribunal_justice`); courts tagged from their constituting Act. (b) DERIVED: compile computes `functions_derived` from the powers a body holds (regulation ← licence+enforcement/sanction; judicial ← adjudication; …), separate from the sourced field, never written into `/data` (A2.8) — it independently corroborates the DBT-sourced `regulation` axis and finds one it missed (the SoS `funding` function). (c) `power_type` **v0.3** gained `adjudication` (a court decides) and `application_to_court` (an actor petitions), closing the `other`-overload that hid the judicial layer. Third-party taxonomies (IfG *Read Before Burning*, #24) remain discovery/differential only. |
| 33 | Breadcrumb tracking — from a method note to a measured register | Systematic `Provision.references` population (the #27 follow-on) | **Closed 2026-07-20 — BUILT and now automatic.** `fetch_legislation.py` mines each provision's outbound references from the OPERATIVE text (CLML marks up `<Citation>` only in amendment commentary, so internal cross-refs are plain text). `pipeline/breadcrumbs.py` → `issues/breadcrumbs.md` is a DERIVED completeness register (a closed stub disappears by itself, so the count means something): detectors for untied vetoes, dangling references, narrative stubs, orphan instruments and **unmined provisions** (fetched but never extracted — the false-assurance case). `CORRECTLY_UNMINED` suppresses legitimately-empty provisions (amending/constitutive/private-holder), each with a stated reason. Closes the #27 follow-on. |
| 34 | Duplication and conflict across Acts (the same power/duty from different provisions) | — | **OPEN — awaiting build (sponsor-raised 2026-07-21).** UK statute overlaps and conflicts across decades; the register will accrue genuine duplicates (same authority conferred by two provisions) and genuine conflicts (a growth duty vs an environmental duty). Do NOT silently dedupe. **Sponsor direction: flag on the card (top-right, or in Sources) where the same right/power derives from different provisions/Acts**, and surface conflicts rather than hide them. Connects directly to **U15 (duty-conflict register)**, which the plan already carries. Design item; not yet built. |
| 35 | New `veto_type` term for a bare power to direct another body NOT to act | Propose a term when `other` recurs (vocab rule) | **OPEN — awaiting sponsor.** `veto_type: other` now recurs for "B may direct A not to act, and A shall comply" (WIA s.16A CMA direction; WIA s.17J(5) SoS direction). It is neither a consent (silence lets A proceed) nor an objection power. Two instances is the vocab's own threshold for proposing a term rather than absorbing it. Proposed: **`direction_not_to_act`**. Sponsor to confirm before adding to the enum. |
| 36 | `outstanding_effects` (consolidation-lag flag) — wire it or disclose the caveat | Add an outstanding-effects check to the fetch step, or accept and disclose | **OPEN — work item.** Every Provision asserts `outstanding_effects=false` with a note "check not yet wired", EXCEPT WIA s.16A which was flagged by hand (legislation.gov.uk shows pending amendments). So the field currently makes an UNSUPPORTED claim on 40+ records — the same failure shape as the 8/8 hard_stop. Decision needed: wire the check into `fetch_legislation.py` (legislation.gov.uk exposes a "changes to legislation" warning per section), or set the field null + disclose the caveat until the currency job (Annex A2.6) exists. |

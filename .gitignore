__pycache__/
*.pyc
.DS_Store

# Raw API page cache — local provenance snapshot, not committed.
# The curated SourceRecord in data/sources/ is committed; the bulky raw
# pages it points to are reproducible by re-running ingest_organisations.py.
data/sources/raw/

# Compiled SQLite is a regenerable binary (pipeline/compile.py from data/).
# graph.json and manifest.json ARE committed — they are the map/data contract.
compiled/*.sqlite

# Internal codebase-review working note — not for the public repo.
Review of Codebase.txt
